[project]
name = "hamlet-mcp"
version = "0.1.0"
description = "MCP wrapper for HAMLET Gradio OR agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "gradio-client>=1.0.0",
  "mcp>=1.0.0",
  "requests>=2.31.0",
]

[project.scripts]
hamlet-mcp = "hamlet_mcp:main"
